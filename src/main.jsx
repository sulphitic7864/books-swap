import React,{useState}from'react';import{createRoot}from'react-dom/client';import{BrowserRouter,Link,NavLink,Navigate,Route,Routes,useNavigate,useParams}from'react-router-dom';import{AppProvider,useApp}from'./hooks/useAppState.jsx';import'./styles/styles.css';
import logo from './assets/bookswap_logo.svg';
const img=(n)=>new URL(`./assets/${n}`,import.meta.url).href;const genres=['Fiction','Mystery','Science','History','Design','Children','Biography','Travel'];const conditions=['Like New','Very Good','Good','Fair'];const avails=['Available','Reserved','Exchanged'];
function Layout(){const{user,logout,toast}=useApp();const[open,setOpen]=useState(false);return <><header className="top"><Link to="/" className="brand"><img src={logo}/><span>BookSwap</span></Link><button className="menu" onClick={()=>setOpen(!open)}>Menu</button><nav className={open?'show':''} onClick={()=>setOpen(false)}>{['Home:/','Browse:/books','Community:/community'].map(x=>{let[a,b]=x.split(':');return <NavLink key={b} to={b}>{a}</NavLink>})}{user&&<><NavLink to="/dashboard">Dashboard</NavLink><NavLink to="/requests">Requests</NavLink><NavLink to="/profile">Profile</NavLink></>}{user?<button onClick={logout}>Sign out</button>:<NavLink className="btn small" to="/login">Sign in</NavLink>}</nav></header><main className="wrap"><Routes>{routes}</Routes></main>{toast&&<div className="toast" role="status">{toast}</div>}</>}
function Protect({children}){const{isSignedIn}=useApp();return isSignedIn?children:<Navigate to="/login" replace/>}function Owner({children}){const{user,state}=useApp();const{id}=useParams();return user&&state.books.find(b=>b.id===id)?.ownerId===user.id?children:<Navigate to="/books" replace/>}
function Landing(){return <><section className="hero"><div><p className="eyebrow">Share a shelf. Start a story.</p><h1>Your next great read may already be nearby.</h1><p>List books you are ready to share, discover titles from other readers, and manage every exchange from one calm community space.</p><div className="actions"><Link className="btn" to="/books">Browse available books</Link><Link className="btn ghost" to="/register">Create an account</Link></div></div><div className="heroCard"><BookMini id="b1"/><BookMini id="b3"/><BookMini id="b8"/></div></section><section className="grid three"><Info title="Location-aware exchanges" text="Each listing and request carries a neighborhood so handovers are easy to plan."/><Info title="Local demo state" text="Try requests, listings, profile edits, and reset everything any time."/><Info title="Calm responsive UI" text="Designed for desktop, tablet, and mobile review widths without backend services."/></section></>}
function BookMini({id}){const{state}=useApp();const b=state.books.find(x=>x.id===id);return <div className="mini"><img src={img(b.cover)}/><b>{b.title}</b><small>{b.location}</small></div>}function Info(p){return <article className="card"><h3>{p.title}</h3><p>{p.text}</p></article>}
function Auth({mode}){const{login,register}=useApp();const nav=useNavigate();const[f,setF]=useState({name:'',email:'',password:'',confirm:'',neighborhood:''});const[err,setErr]=useState('');function submit(e){e.preventDefault();try{setErr('');if(mode==='register'){let errors=[];if(f.name.length<2)errors.push('Full name must be at least 2 characters.');if(!/^[^@]+@[^@]+\.[^@]+$/.test(f.email))errors.push('Enter a valid email.');if(!/(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}/.test(f.password))errors.push('Password needs 8 characters, uppercase, lowercase, and a number.');if(f.password!==f.confirm)errors.push('Passwords must match.');if(f.neighborhood.length<2)errors.push('Neighborhood is required.');if(errors.length)throw Error(errors.join(' '));register(f)}else login(f.email,f.password);nav('/dashboard')}catch(ex){setErr(ex.message)}}return <section className="auth"><form className="panel" onSubmit={submit}><h1>{mode==='register'?'Create your BookSwap account':'Welcome back'}</h1><p className="muted">{mode==='login'?'Use a supplied demo account or create a local profile.':'Join with a neighborhood so exchanges stay practical.'}</p>{err&&<div className="error">{err}</div>}{mode==='register'&&<Input label="Full name" value={f.name} onChange={v=>setF({...f,name:v})}/>}<Input label="Email" type="email" value={f.email} onChange={v=>setF({...f,email:v})}/><Input label="Password" type="password" value={f.password} onChange={v=>setF({...f,password:v})}/>{mode==='register'&&<><Input label="Confirm password" type="password" value={f.confirm} onChange={v=>setF({...f,confirm:v})}/><Input label="Neighborhood" value={f.neighborhood} onChange={v=>setF({...f,neighborhood:v})}/></>}<button className="btn">{mode==='register'?'Create account':'Sign in'}</button><p className="muted">Demo: maya@demo.local / ReadMore8</p></form></section>}
function Input({label,value,onChange,type='text',readOnly=false}){return <label className="field"><span>{label}</span><input readOnly={readOnly} type={type} value={value} onChange={e=>onChange(e.target.value)}/></label>}
function Select({label,value,onChange,children}){return <label className="field"><span>{label}</span><select value={value} onChange={e=>onChange(e.target.value)}>{children}</select></label>}
function Catalog(){const{state}=useApp();const[q,setQ]=useState(''),[g,setG]=useState(''),[a,setA]=useState(''),[l,setL]=useState(''),[sort,setSort]=useState('newest');let locs=[...new Set(state.books.map(b=>b.location))];let books=state.books.filter(b=>(b.title+b.author).toLowerCase().includes(q.toLowerCase())&&(!g||b.genre===g)&&(!a||b.availability===a)&&(!l||b.location===l));books.sort((x,y)=>sort==='title'?x.title.localeCompare(y.title):sort==='author'?x.author.localeCompare(y.author):new Date(y.addedOn)-new Date(x.addedOn));return <><Heading title="Browse shared books" text="Search nearby shelves by title, author, genre, availability, and exchange location."/><div className="filters"><input placeholder="Search by title or author" value={q} onChange={e=>setQ(e.target.value)}/><select value={g} onChange={e=>setG(e.target.value)}><option value="">All genres</option>{genres.map(x=><option key={x}>{x}</option>)}</select><select value={a} onChange={e=>setA(e.target.value)}><option value="">All availability</option>{avails.map(x=><option key={x}>{x}</option>)}</select><select value={l} onChange={e=>setL(e.target.value)}><option value="">All locations</option>{locs.map(x=><option key={x}>{x}</option>)}</select><select value={sort} onChange={e=>setSort(e.target.value)}><option value="newest">Newest</option><option value="title">Title A to Z</option><option value="author">Author A to Z</option></select><button onClick={()=>{setQ('');setG('');setA('');setL('')}}>Clear filters</button></div>{books.length?<div className="books">{books.map(b=><BookCard key={b.id} b={b}/>)}</div>:<Empty text="No books match the current search and filters."/>}</>}
function BookCard({b}){const{state}=useApp();const o=state.users.find(u=>u.id===b.ownerId);return <Link className="book card" to={`/books/${b.id}`}><img src={img(b.cover)}/><div><span className="badge">{b.availability}</span><h3>{b.title}</h3><p>{b.author}</p><small>{b.genre} • {b.condition}</small><small>Owner: {o?.name} • {b.location}</small></div></Link>}
function Heading({title,text}){return <div className="heading"><h1>{title}</h1>{text&&<p>{text}</p>}</div>}function Empty({text}){return <div className="empty">{text}</div>}
function BookDetail(){const{id}=useParams();const{state,user,createRequest}=useApp();const b=state.books.find(x=>x.id===id);const[open,setOpen]=useState(false);if(!b)return <NotFound/>;const owner=state.users.find(u=>u.id===b.ownerId);const can=user&&user.id!==b.ownerId&&b.availability==='Available'&&!state.requests.some(r=>r.bookId===b.id&&r.requesterId===user.id&&['Pending','Accepted'].includes(r.status));return <section className="detail"><img className="coverBig" src={img(b.cover)}/><div><span className="badge">{b.availability}</span><h1>{b.title}</h1><h2>{b.author}</h2><p>{b.description}</p><dl><dt>Genre</dt><dd>{b.genre}</dd><dt>Condition</dt><dd>{b.condition}</dd><dt>Owner</dt><dd>{owner?.name}</dd><dt>Exchange location</dt><dd>{b.location}</dd></dl><div className="actions">{user?.id===b.ownerId&&<Link className="btn ghost" to={`/books/${b.id}/edit`}>Edit listing</Link>}{user?<button className="btn" disabled={!can} onClick={()=>setOpen(true)}>{can?'Request exchange':'Request unavailable'}</button>:<Link className="btn" to="/login">Sign in to request</Link>}</div></div>{open&&<RequestDialog book={b} onClose={()=>setOpen(false)} onSend={(m)=>{createRequest(b.id,m);setOpen(false)}}/>}</section>}
function RequestDialog({book,onClose,onSend}){const[msg,setMsg]=useState('');return <div className="modal" role="dialog" aria-modal="true"><form className="dialog" onSubmit={e=>{e.preventDefault();onSend(msg)}}><h2>Request exchange</h2><p>Exchange location: <b>{book.location}</b></p><label className="field"><span>Optional message</span><textarea maxLength="240" value={msg} onChange={e=>setMsg(e.target.value)}/></label><div className="actions"><button className="btn">Send request</button><button type="button" onClick={onClose}>Close</button></div></form></div>}
function Dashboard(){const{state,user}=useApp();let my=state.books.filter(b=>b.ownerId===user.id),sent=state.requests.filter(r=>r.requesterId===user.id&&['Pending','Accepted'].includes(r.status)),rec=state.requests.filter(r=>r.ownerId===user.id&&['Pending','Accepted'].includes(r.status)),done=state.requests.filter(r=>[r.requesterId,r.ownerId].includes(user.id)&&r.status==='Completed');return <><Heading title="Your BookSwap dashboard" text="Track listings, requests, and completed community exchanges."/><div className="grid four">{[['My books',my.length],['Sent requests',sent.length],['Received requests',rec.length],['Completed exchanges',done.length]].map(x=><div className="metric card" key={x[0]}><span>{x[0]}</span><b>{x[1]}</b></div>)}</div><div className="actions"><Link className="btn" to="/books/new">Add a book</Link><Link className="btn ghost" to="/books">Browse books</Link><Link className="btn ghost" to="/requests">Review requests</Link></div><h2>My books</h2><div className="books">{my.map(b=><BookCard key={b.id} b={b}/>)}</div></>}
function BookForm({edit=false}){const{id}=useParams();const{state,user,addBook,updateBook,removeBook}=useApp();const nav=useNavigate();const existing=state.books.find(b=>b.id===id);const[f,setF]=useState(existing||{title:'',author:'',genre:'',condition:'',availability:'Available',description:'',cover:'cover_01_lantern_atlas.jpg',location:user.neighborhood});const[err,setErr]=useState('');function save(e){e.preventDefault();let er=[];if(!f.title||f.title.length>100)er.push('Title is required.');if(!f.author||f.author.length>80)er.push('Author is required.');if(!f.genre)er.push('Genre is required.');if(!f.condition)er.push('Condition is required.');if(!f.availability)er.push('Availability is required.');if(!f.location||f.location.length<2)er.push('Exchange location is required.');if(f.description.length<20||f.description.length>500)er.push('Description must be 20 to 500 characters.');if(er.length){setErr(er.join(' '));return} edit?(updateBook(id,f),nav(`/books/${id}`)):nav(`/books/${addBook(f)}`)}return <form className="panel form" onSubmit={save}><h1>{edit?'Edit book listing':'Add a book'}</h1>{err&&<div className="error">Review the highlighted fields and try again. {err}</div>}<div className="formgrid"><Input label="Title" value={f.title} onChange={v=>setF({...f,title:v})}/><Input label="Author" value={f.author} onChange={v=>setF({...f,author:v})}/><Select label="Genre" value={f.genre} onChange={v=>setF({...f,genre:v})}><option value="">Select genre</option>{genres.map(x=><option key={x}>{x}</option>)}</Select><Select label="Condition" value={f.condition} onChange={v=>setF({...f,condition:v})}><option value="">Select condition</option>{conditions.map(x=><option key={x}>{x}</option>)}</Select><Select label="Availability" value={f.availability} onChange={v=>setF({...f,availability:v})}>{avails.map(x=><option key={x}>{x}</option>)}</Select><Input label="Exchange location" value={f.location} onChange={v=>setF({...f,location:v})}/><Select label="Cover" value={f.cover} onChange={v=>setF({...f,cover:v})}>{state.books.map(b=><option key={b.cover} value={b.cover}>{b.cover}</option>)}</Select></div><img className="preview" src={img(f.cover)}/><label className="field"><span>Description</span><textarea value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></label><div className="actions"><button className="btn">Save listing</button>{edit&&<button type="button" className="danger" onClick={()=>{if(confirm('Remove this book listing?')){removeBook(id);nav('/dashboard')}}}>Remove book</button>}</div></form>}
function Requests({history=false}){const{state,user,requestAction}=useApp();const[filter,setFilter]=useState('');let closed=['Completed','Rejected','Cancelled'];let reqs=state.requests.filter(r=>[r.requesterId,r.ownerId].includes(user.id)&&(history?closed.includes(r.status):!closed.includes(r.status)));if(filter)reqs=reqs.filter(r=>r.status===filter);return <><Heading title={history?'Request history':'Exchange requests'} text={history?'Closed records stay visible for review.':'Sent and received active requests are separated by participant role.'}/>{history&&<select className="statusFilter" value={filter} onChange={e=>setFilter(e.target.value)}><option value="">All statuses</option>{closed.map(x=><option key={x}>{x}</option>)}</select>}<div className="requestList">{reqs.map(r=><RequestRow key={r.id} r={r} user={user} state={state} action={requestAction}/>)}</div></>}
function RequestRow({r,user,state,action}){const b=state.books.find(x=>x.id===r.bookId),requester=state.users.find(x=>x.id===r.requesterId),owner=state.users.find(x=>x.id===r.ownerId);return <article className="request card"><div><span className="badge">{r.status}</span><h3>{b?.title}</h3><p>{r.message}</p><small>Requester: {requester?.name} • Owner: {owner?.name}</small><small>Location: {b?.location} • Updated {r.updatedOn}</small></div><div className="actions">{r.status==='Pending'&&r.ownerId===user.id&&<><button onClick={()=>action(r.id,'accept')}>Accept</button><button onClick={()=>action(r.id,'reject')}>Reject</button></>}{r.status==='Pending'&&r.requesterId===user.id&&<button onClick={()=>action(r.id,'cancel')}>Cancel</button>}{r.status==='Accepted'&&r.ownerId===user.id&&<button onClick={()=>action(r.id,'complete')}>Complete</button>}</div></article>}
function Profile(){const{user,updateUser,reset}=useApp();const[f,setF]=useState(user);return <form className="panel form" onSubmit={e=>{e.preventDefault();updateUser(f)}}><h1>Profile management</h1><Input label="Full name" value={f.name} onChange={v=>setF({...f,name:v})}/><Input label="Email" value={f.email} readOnly onChange={()=>{}}/><Input label="Neighborhood" value={f.neighborhood} onChange={v=>setF({...f,neighborhood:v})}/><label className="field"><span>Bio</span><textarea maxLength="220" value={f.bio} onChange={e=>setF({...f,bio:e.target.value})}/></label><div className="actions"><button className="btn">Save profile</button><button type="button" className="danger" onClick={()=>confirm('Reset all demo data and sign out?')&&reset()}>Reset demo data</button></div></form>}
function Community(){const{state}=useApp();const[f,setF]=useState('');let list=state.activity.filter(a=>!f||a.type===f).sort((a,b)=>new Date(b.occurredOn)-new Date(a.occurredOn));return <><Heading title="Community activity" text="Follow new listings, exchanges, and neighborhood milestones."/><div className="tabs">{[['','All activity'],['listing','New listings'],['exchange','Exchanges'],['milestone','Milestones']].map(x=><button className={f===x[0]?'active':''} onClick={()=>setF(x[0])} key={x[1]}>{x[1]}</button>)}</div><div>{list.map(a=><article className="activity card" key={a.id}><b>{a.text}</b><small>{new Date(a.occurredOn).toLocaleString()}</small></article>)}</div></>}
function NotFound(){return <div className="empty"><h1>Page not found</h1><p>This shelf is empty.</p><Link className="btn" to="/books">Browse</Link></div>}
const routes=<><Route path="/" element={<Landing/>}/><Route path="/login" element={<Auth mode="login"/>}/><Route path="/register" element={<Auth mode="register"/>}/><Route path="/books" element={<Catalog/>}/><Route path="/books/:id" element={<BookDetail/>}/><Route path="/community" element={<Community/>}/><Route path="/dashboard" element={<Protect><Dashboard/></Protect>}/><Route path="/profile" element={<Protect><Profile/></Protect>}/><Route path="/books/new" element={<Protect><BookForm/></Protect>}/><Route path="/books/:id/edit" element={<Protect><Owner><BookForm edit/></Owner></Protect>}/><Route path="/requests" element={<Protect><Requests/></Protect>}/><Route path="/requests/history" element={<Protect><Requests history/></Protect>}/><Route path="*" element={<NotFound/>}/></>;
createRoot(document.getElementById('root')).render(<BrowserRouter><AppProvider><Layout/></AppProvider></BrowserRouter>);
